# Core, required to run the offline demo + tests
numpy>=1.24
pytest>=7.0

# Optional, real semantic embeddings (set AEGIS_EMBEDDER=minilm)
# sentence-transformers>=2.2

# Optional, real LLM (set AEGIS_LLM=openai | anthropic)
# openai>=1.0
# anthropic>=0.30

# Real data protection (set AEGIS_PROTECTOR=protegrity):
protegrity-developer-python>=1.1.1
